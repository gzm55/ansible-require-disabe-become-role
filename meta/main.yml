galaxy_info:
  author: James Z.M. Gao
  description: Assert the task can force disable become action.

  license: BSD

  min_ansible_version: 2.0

  platforms:
  - name: GenericBSD
    versions:
    - any
  - name: GenericLinux
    versions:
    - any
  - name: Windows
    versions:
    - all
  - name: GenericUNIX
    versions:
    - any

  galaxy_tags: [system]

allow_duplicates: no
dependencies:
- role: gzm55.local_ansible_config
  config_key: privilege_escalation/become_allow_same_user
