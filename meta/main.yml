galaxy_info:
  author: James Z.M. Gao
  description: Assert the task can force disable become action.

  license: BSD

  ## direct lookup ansible config
  min_ansible_version: 2.5

  platforms:
  - name: GenericBSD
    versions:
    - any
  - name: GenericLinux
    versions:
    - any
  - name: Windows
    versions:
    - all
  - name: GenericUNIX
    versions:
    - any

  galaxy_tags: [system]

allow_duplicates: no
dependencies:
- role: gzm55.local_id_plugin
  version: master
